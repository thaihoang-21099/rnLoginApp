import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    container:{
        flex: 1, 
        alignItems: 'center', 
        //padding: 10,
        paddingTop:20
    },
    image:{
        width: '100%', 
        height: 80,
        marginBottom:10
    }
})

export default styles;